package MODEL;

public class ProdottoTester {
	/*
	 ProdottoBean pr = new ProdottoBean();
		ProdottoImplementazioneDAO primp = new ProdottoImplementazioneDAO();
		short codice = 49;
		
		pr.setCodice(codice);
		
		pr.setDescrizione("prodotto fittizzio ");
		pr.setNome("vaffanculo a eva la stronza");
		pr.setPrezzo(25);
		pr.setQuantita(2);
		pr.setTipo(MODEL.ProdottoBean.categoria.Altro);
		
		//primp.inserisciProdotto(pr);
		//primp.modificaDescrizione(pr);
		//primp.modificaNome(pr);
		//primp.modificaPrezzo(pr);
		//primp.modificaTipo(pr);
		//primp.modificaQuantita(pr);
		//primp.decrementaQuantita(pr);
		
		primp.leggiSingoloProdotto(pr);
		response.getWriter().append("Served at: ").append("\ncodice: " + pr.getCodice());
		response.getWriter().append("Served at: ").append("\n nome " + pr.getNome());
		response.getWriter().append("Served at: ").append("\n prezzo; " + pr.getPrezzo());
		response.getWriter().append("Served at: ").append("\n descrizione: " + pr.getDescrizione());
		response.getWriter().append("Served at: ").append("\n quantità: " + pr.getQuantita());
		response.getWriter().append("Served at: ").append("\n tipo " + pr.getTipo().toString());
		
		//primp.rimuoviProdotto(pr);
		
		List<ProdottoBean> prod = primp.leggiProdotto();
		for (ProdottoBean ps : prod ) {
			response.getWriter().append("\ncodice: " + ps.getCodice());
			response.getWriter().append("\n nome " + ps.getNome());
			response.getWriter().append("\n prezzo; " + ps.getPrezzo());
			response.getWriter().append("\n descrizione: " + ps.getDescrizione());
			response.getWriter().append("\n quantità: " + ps.getQuantita());
			response.getWriter().append("\n tipo " + ps.getTipo().toString());
		}
		
	  */
}
