package model;

	public class TelefonoTester {
	/*
	 * TelefonoBEAN tel = new TelefonoBEAN();
			UtenteBean ut = new UtenteBean();
			ut.setEmail("G.DEROSA80@USER.IT");
			
			TelefonoImplementatoDAO telimp = new TelefonoImplementatoDAO();
			//tel.setNumero("3587492040");
			//tel.setUtente(ut);
			//telimp.aggiungiTelefono(tel);
			//telimp.rimuoviTelefono(tel);
			List<TelefonoBEAN> prod = telimp.leggiTelefono(ut);
			for (TelefonoBEAN ps : prod ) {
				response.getWriter().append(ps.getNumero());
			}
	 * */
}
